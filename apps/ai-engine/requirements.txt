# ==========================================
# AI ENGINE REQUIREMENTS
# Environment: Python 3.10+ | CUDA 12.x
# ==========================================

# --- CRITICAL PRE-REQUISITE ---
# Please install PyTorch with CUDA support MANUALLY before installing this file.
# Run: pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# ------------------------------

# --- Core AI & ASR (Speech Recognition) ---
# Optimized Whisper implementation using CTranslate2 (Faster than OpenAI's original)
faster-whisper>=1.2.1

# Timestamp Alignment & Hallucination Fixes
# Used for refining long segments (Word-level timestamps)
stable-ts>=2.19.1

# Voice Activity Detection (VAD)
# Efficiently detects speech vs silence. Requires onnxruntime-gpu for CUDA acceleration.
# Note: silero-vad 4.x does not exist, use 5.1.2+ (stable) or 6.x (latest)
silero-vad>=5.1.2
onnxruntime-gpu>=1.23.0

# --- Audio Processing ---
# Standard libraries for audio manipulation and I/O
librosa>=0.11.0
soundfile>=0.12.1
numpy>=1.26.0

# FFmpeg wrapper for Python (Requires FFmpeg installed on OS)
ffmpeg-python>=0.2.0

# --- Translation ---
# Tool for translating text (Placeholder for future Offline Model)
deep-translator>=1.11.4

# --- System, Config & Logging ---
# For loading .env variables
python-dotenv>=1.0.0

# Modern logging library (Better than standard 'logging')
loguru>=0.7.0

# Data validation and settings management
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- Development Utilities ---
# Type checking and linting (Optional but recommended)
types-requests